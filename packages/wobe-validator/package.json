{
	"name": "wobe-validator",
	"version": "0.1.0",
	"main": "index.ts",
	"dependencies": { "@sinclair/typebox": "0.32.20" },
	"devDependencies": {
		"wobe": "workspace:*"
	},
	"scripts": {
		"format": "biome format --write .",
		"lint": "biome lint ./**/**/*.ts --no-errors-on-unmatched --config-path=../../",
		"build": "tsc --project tsconfig.json",
		"dev": "bun run --watch dev/index.ts"
	}
}
