{
	"name": "wobe",
	"version": "0.1.0",
	"main": "index.ts",
	"devDependencies": {
		"get-port": "7.0.0"
	},
	"scripts": {
		"format": "biome format --write .",
		"lint": "biome lint ./**/**/*.ts --no-errors-on-unmatched --config-path=../../",
		"ci": "bun lint && bun test && bun test:node",
		"test:node": "NODE_TEST='true' bun test",
		"build": "tsc --project tsconfig.json",
		"dev": "bun run --watch dev/index.ts"
	}
}
