import { run, bench } from 'mitata'

bench('fetch', async () => {
	// const tata = req
	// object['key'] = 'value'
	// const tata = object['key']
	// await fetch('http://127.0.0.1:3000/')
})

await run()
