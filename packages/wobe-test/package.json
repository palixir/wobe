{
	"name": "wobe-test",
	"version": "1.0.0",
	"description": "Wobe description",
	"author": {
		"name": "coratgerl",
		"url": "https://github.com/coratgerl"
	},
	"license": "MIT",
	"main": "dist/index.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/palixir/wobe"
	},
	"devDependencies": {},
	"scripts": {
		"generate:types": "dts-bundle-generator $(pwd)/src/index.ts -o dist/index.d.ts --no-check",
		"build": "bun build --minify --outdir dist $(pwd)/src/index.ts --target=bun && bun generate:types",
		"release": "semantic-release -e ../../.releaserc -e semantic-release-monorepo"
	}
}
