commit-msg:
  commands:
    conventional:
      run: bun commitlint --edit $1

pre-commit:
  parallel: true
  commands:
    check:
      glob: '*.{js,ts,jsx,tsx}'
      run: bun oxlint check {staged_files}
